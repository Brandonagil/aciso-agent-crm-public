# .env.example
# Firebase Client Config (Prefix with NEXT_PUBLIC_ to expose to browser)
NEXT_PUBLIC_FIREBASE_API_KEY=your-firebase-api-key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-firebase-auth-domain
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-firebase-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-firebase-storage-bucket
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your-firebase-messaging-sender-id
NEXT_PUBLIC_FIREBASE_APP_ID=your-firebase-app-id
# NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=YOUR_MEASUREMENT_ID # Optional
# Firebase Admin SDK Config (Server-side only, NO NEXT_PUBLIC_ prefix)
FIREBASE_ADMIN_PROJECT_ID=your-firebase-project-id
FIREBASE_ADMIN_CLIENT_EMAIL=your-firebase-client-email
# IMPORTANT: Format the private key correctly for environment variables
# Replace literal newlines in the JSON key with '\\n'
FIREBASE_ADMIN_PRIVATE_KEY=your-firebase-admin-private-key
# ADK Integration Configuration
# Local ADK Agent URL (dev environment)
DATA_SCIENCE_AGENT_URL=http://127.0.0.1:8001
# For Cloud Run, set DATA_SCIENCE_AGENT_URL to the deployed service.
# Set to 'true' to use Cloud ADK, 'false' for local
USE_CLOUD_ADK=false
# URL where the frontend is deployed (used for internal API calls)
NEXT_PUBLIC_DEPLOY_URL=http://localhost:3000
# Auth control for ADK
# Keep authentication enabled. Existing .env.local files are unchanged.
NO_ADK_AUTH=false
# Debug settings
DEBUG_ADK_INTEGRATION=false
# Add other environment variables as needed
# Optional raw JSON, not base64. Keep real credentials outside Git.
GOOGLE_APPLICATION_CREDENTIALS_JSON=

# E2E Test credentials (used by Playwright tests; never commit real values)
TEST_USER_EMAIL=your-test-user@example.com
TEST_USER_PASSWORD=your-test-password

# Google Cloud / BigQuery (server-side only)
# Set one project variable. BQ_PROJECT_ID takes precedence if both are set.
GOOGLE_CLOUD_PROJECT=your-google-cloud-project-id
# BQ_PROJECT_ID=your-bigquery-project-id
# Optional. If blank, the existing dataset discovery uses the configured project.
BQ_DATASET_ID=your_bigquery_dataset
# Optional absolute path to your own key file. Blank uses Application Default Credentials.
GOOGLE_APPLICATION_CREDENTIALS=
